/**
 * Application User state
 */
// Actions

// Reducers
const initialState = {};
export default user = (state = initialState, action) => {
	switch (action) {
		default:
			return state;
	}
};

// Action Creators

// Side Effects
