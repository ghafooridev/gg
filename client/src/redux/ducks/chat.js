/**
 * Application User state
 */
// Actions

// Reducers
const initialState = {};
export default chat = (state = initialState, action) => {
	switch (action) {
		default:
			return state;
	}
};

// Action Creators

// Side Effects
