/**
 * Application User state
 */
// Actions

// Reducers
const initialState = {};
const chatReducer = (state = initialState, action) => {
	switch (action) {
		default:
			return state;
	}
};

// Action Creators

// Side Effects

export default chatReducer;
