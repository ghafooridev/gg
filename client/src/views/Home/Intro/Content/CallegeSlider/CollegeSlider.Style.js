import { makeStyles } from "@material-ui/core/styles"

export const styles = makeStyles((theme) => ({
  root: {
    marginBottom: 100,
  },
  title: {
    textAlign: "center",
    margin: "75px 0 50px",
  },
  image: {
    width: 130,
    height: 100,
  },
}))
